# Secrets — encrypted at rest, never returned via the API. Accessed by shared-pool
# functions via context['secrets'][NAME]. Value is interpolated from .env at deploy time.
secrets:
  - name: MOONLIT_KEY
    value: ${MOONLIT_KEY}
    description: Moonlit Legal Research REST API subscription key (api.moonlit.ai/v1.1)
